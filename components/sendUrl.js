dbConfig = {
    host     : 'localhost',
    user     : 'root',
    password : '1234',
    port     : 3306,
    database : 'degree',
    // connectionLimit: 200
};
export default function sendGetUrl(method, url) {
  const date = parseISO(dateString)
  return <time dateTime={dateString}>{format(date, 'LLLL d, yyyy')}</time>
}